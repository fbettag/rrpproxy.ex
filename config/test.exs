use Mix.Config

# Configure your access
config :rrpproxy,
  username: "bettag",
  password: "w00tw00tOTE",
  ote: true,
  # Only required for tests to test transfers
  xfer_username: "bettag_test",
  xfer_password: "8lW,u$zmZu;:"
